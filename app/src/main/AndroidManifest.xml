<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e02view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AlertsActivity" android:label="@string/action_spinners"></activity>
        <activity
            android:name=".SpinnersActivity"
            android:label="@string/action_spinners" />
        <activity android:name=".CheckboxesActivity" />
        <activity
            android:name=".ButtonActivity"
            android:label="@string/action_buttons" />
        <activity
            android:name=".MemoActivity"
            android:label="@string/action_memo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>